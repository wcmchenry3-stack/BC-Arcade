EXPO_PUBLIC_API_URL=https://your-api-url.example.com
EXPO_PUBLIC_SENTRY_DSN=
# EXPO_PUBLIC_SENTRY_ENVIRONMENT is deliberately NOT set here: the Sentry environment
# follows EXPO_PUBLIC_API_URL (src/utils/sentryConfig.ts). A value in a tracked env
# file overrides that rule for every build that loads the file (#851).
EXPO_PUBLIC_FEEDBACK_WORKER_URL=https://feedback-worker.wcmchenry3.workers.dev
