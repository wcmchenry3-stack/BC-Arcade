# BC Arcade — Store Submission Game Plan (target: submit by Oct 15, 2026)

## Context

BC Arcade v1.0.9 (build 10009) has never been submitted to either store. Epic #1916 (first-submission readiness) and epic #821 (productionization) track the gaps. The goal: **submit to both the Apple App Store and Google Play by Oct 15**, working day-by-day from Sep 18 with two machines (1 Mac, 1 PC) available every day.

### Decisions made (with the user, 2026-09-17)

1. **Premium games are hidden entirely in v1.0** — not shown locked, not free. The 6 premium games (yacht, cascade, hearts, sudoku, starswarm, sort) disappear from the store build; the 6 free games ship (blackjack, solitaire, freecell, mahjong, daily word, twenty48). IAP comes post-launch (epic #822). This resolves blocker #1917 without building IAP and without ever releasing premium content free. **Amended 2026-09-19 (owner): all 12 games stay visible and free in pre-launch builds.** A build compiled against the pre-launch (dev) API — internal TestFlight / Play test builds and the `dev`-branch staging site — shows every game, and the dev backend already grants every premium game to every session (`ENTITLEMENT_DEV_OVERRIDE`). A build compiled against anything else is a store build and hides the six. **There is no flag to flip back before launch**: pointing the release config at the production API (Tue 29) hides the games and ends the free entitlements in one step (#2417, `ARCHITECTURE.md` §10.7). Nothing premium is ever free in a store build.
2. **Guideline 4.2 mitigation = XP + daily challenge** (#1914 Tier 1 minus Game Center): server-side Arcade XP/player level, one daily cross-game challenge over free games, home-screen progression, strong reviewer notes.
3. **Store accounts + app records already exist** in App Store Connect and Play Console; app has been on internal/closed testing before. No Play 14-day cold-start blocker assumed (verify Day 1).
4. **Quality bar for launch = crash/stability fixes only.** i18n gaps, a11y backlog, and CI hardening are explicitly deferred post-launch (except where a hidden game makes the issue moot).
5. **Production database = Supabase; dev database stays on Render; local + CI stay on SQLite** (owner, 2026-09-20). Supabase is prod-only — nothing else points at it. **It is a fresh database, not a migration**: prod has never existed, so nothing is copied from dev (no test scores, XP or dev-override entitlements in launch-day data); `alembic upgrade head` builds the schema and every seed row. Supabase runs on the **free plan through setup and QA, and moves to Pro before the Oct 9 submission** (free projects pause after ~7 idle days and have no backups). **Sentry keeps one project per platform and splits dev from prod by the `environment` tag** — no separate prod DSN. Design in section D.

### Current ground truth (verified in code, 2026-09-17)

- ✅ Already fixed: iOS mic usage description (#1919), Android RECORD_AUDIO/SYSTEM_ALERT_WINDOW (#1920), iOS Privacy Manifest declares Sentry crash/perf/diagnostics (#1921), data deletion (#1923). Sentry integrated both sides, `sendDefaultPii: false` — **but see the Sep 20 Sentry finding below: dev traffic is tagged `production`.**
- ❌ **#1918/#2277 keystore password still committed** in `frontend/android/gradle.properties` (lines 72 + 74), file tracked in git, not gitignored. Findings Sep 18: **this repo is public**, so the value is permanently burned; the keystore *file* was never committed on any branch (password is useless without it), so a password change suffices — no Play upload-key reset needed unless the file has ever left the owner's machines. Keystore is PKCS12 (one password covers store + key), alias `upload`. No workflow reads any `ANDROID_*` secret — release signing is local-only; CI's release smoke uses a throwaway debug key. Seven secrets exist: `ANDROID_UPLOAD_*` ×3 (to be refreshed) and a legacy unused `ANDROID_KEYSTORE_*`/`ANDROID_KEY_*` ×4 (delete after rotation). History will **not** be scrubbed (would require force-pushing protected branches; rotation makes the old value worthless).
- ❌ **No privacy/terms links** in `frontend/src/screens/SettingsScreen.tsx`; no Privacy Policy or ToS hosted anywhere (#828, #1922).
- ❌ **No IAP code** anywhere (moot — premium games hidden).
- ❌ Locked-game UI live: `LockedGameScreen.tsx`, `makePremiumScreen()` in `frontend/App.tsx` (~130–154), premium set in `frontend/src/entitlements/EntitlementContext.tsx` (~line 25).
- ✅ **Version discrepancy — fixed Sep 19 (#2412)**: iOS `Info.plist` hardcoded 1.0.0 / build 1 while app.json, Android and the pbxproj were at 1.0.9 / 10009 (`version-sync.yml` never patched the plist). The plist now references `$(MARKETING_VERSION)` / `$(CURRENT_PROJECT_VERSION)` — **never hand-edit a version into it**; see `docs/IOS.md` §Version numbers. Side effects of the first post-merge Xcode Cloud build: it uploads as **1.0.9**, opening a new ASC/TestFlight version train (earlier uploads were 1.0.0 — rename the ASC "Prepare for Submission" version to match when it's needed), and the Sentry iOS release id moves from `…@1.0.0+N` to `…@1.0.9+N`.
- ❌ **No prod environment of any kind (verified Sep 20 via the Render + Supabase MCP servers).** Render holds only the dev stack: `bc-arcade-api-dev` (`srv-d7ffvdnlk1mc73ddflsg`), `bc-arcade-frontend-dev` (`srv-d7ffvii8qa3s73dia9eg`) and the Postgres `bc-arcade-db` (`dpg-d7e4ohjeo5us7381qa0g-a`, PG 18, Oregon, `basic_256mb`) in workspace `tea-d6u9vtq4d50c73cp3tp0`. The suspended `gaming-app-*` pair that #505 proposes reusing **no longer exists** — prod services are created fresh. _Later sessions: call `get_service` / `get_postgres` with these IDs; do not re-list the workspace (13 services, ~5k tokens a call)._
- ❌ **`render.yaml` would point prod at the dev database (found Sep 20).** The prod API's `DATABASE_URL` is `fromDatabase: bc-arcade-db` — the name of the _live dev DB_ — and the block is commented "(prod)". The live DB was never blueprint-managed (its real name/user are `gaming_app_52vr` / `gaming_app_user`, not the yaml's). **Do not apply the blueprint as written.** Fixed by design D; `render.yaml` also declares only 3 of the 8 env vars the backend reads (`DATABASE_URL`, `ALLOWED_ORIGINS`, `SENTRY_DSN`, `ENVIRONMENT`, `ENTITLEMENT_PRIVATE_KEY`, `ENTITLEMENT_PUBLIC_KEY`, `DAILY_WORD_SALT`, `ADMIN_API_TOKEN` — plus `DAILY_CHALLENGE_SALT` once C lands).
- ⚠️ **Supabase is a blank slate (Sep 20).** Project `xmdvuyafzoqwuxquxgaj` (still named "wcmchenry3-stack's Project"), org `bc-arcade`, PG 17, `us-west-2` — co-located with Render Oregon. Zero tables. **Free plan: pauses after ~7 idle days, no backups**; `/health` never touches the DB, so Render's health check will not keep it awake (created Sep 20 → could pause by Sep 27 if untouched; a paused project is a one-click restore). Connection rules: Supabase's direct host is IPv6-only and Render has no outbound IPv6, so prod connects through the **session pooler, port 5432** — it supports prepared statements, so asyncpg (`backend/db/base.py`) and Alembic's psycopg2 (`alembic/env.py`) work **with no code change**. **Never the transaction pooler (6543)** — it breaks asyncpg. No `ssl=` / `sslmode=` in the URL (the two drivers disagree on the parameter name); enforce SSL on the Supabase side instead. The Supabase ↔ GitHub link stays inert: there is no `supabase/` directory and **Alembic is the only schema path** (never the MCP `apply_migration`).
- ❌ **Sentry: dev reports _as_ production (found Sep 20; the owner's "prod points at dev" suspicion, reversed).** `frontend/App.tsx` defaults `environment` to `"production"` when `EXPO_PUBLIC_SENTRY_ENVIRONMENT` is unset, and Xcode Cloud's `ci_post_clone.sh` writes only the API URL + DSN — so **every TestFlight build against the dev API is tagged `production`**. `backend/main.py` never passes `environment` (sentry-sdk's default is `production`), so the dev API's errors are tagged `production` too. One DSN is shared by `.env`, `.env.production` and `ci_post_clone.sh` (fine — decision 5 splits by tag). Native `release` is already set automatically by `@sentry/react-native`; the backend sets none. Everything in Sentry's `production` environment before the fix is dev noise — baseline launch health from the first prod-API release, not from history. The Sentry MCP server was authenticated later on Sep 20 and the org audited — next bullet. Fixed by design D (#851).
- ⚠️ **Sentry audit (Sep 20, MCP authenticated).** Org `buffing-chi` (region `https://us.sentry.io`); projects `bc_games` (the app — issue prefix `BC_GAMES-`), `bc_bookshelf` (clean) and `rulersai` (another repo). _Later sessions: `search_issues` with `projectSlugOrId` + a `limit`, `get_sentry_resource` by short ID — never list._ Ten unresolved issues; all seven `BC_GAMES-*` were network failures or simulator noise, none a crash. Findings: (1) **the #2402/#2413 offline fix does not match what Expo throws** — native `fetch` throws `FetchError extends Error` (not `CodedError` / `TypeError`), so `isNetworkError()` misses it, offline errors are still reported as `unexpected`, and the retry / Daily Word cached-puzzle fallbacks still don't fire on device → **#2428**, launch-gating (table below); (2) an Android emulator matching the CI smoke config and iOS simulators report as `production`, and simulators emit App Hang events → folded into design D as **#2429**; (3) network-failure warnings are unthrottled (one iOS device sent 270 events, one web client 1,045) → **#2430**. Resolved in Sentry: 4X, 4Z (simulator hangs) and 2F (stale). Open, waiting on fixes: 3N (#2430), 4W / 4Y / 42 (#2428). RulersAI's Sentry issues are filed in that repo (#667, #668) — not part of this plan. **The Sentry MCP login is per machine** (`/mcp` → claude.ai Sentry — the connector cannot start the OAuth flow itself). PC authenticated Sep 20; **Mac authenticated Sep 20 evening, first Mac triage run:** 3 unresolved in 14d, all tracked, nothing new — 3N (77 events, #2430), 4Y (8, iOS simulator, #2428), 4W (3, CI emulator, #2428 / #2429); 42 had no events in the window.
- ⚠️ **In-app feedback: broken on native, and public where it works (found Sep 20, fact-checking the Privacy Policy; worker source read over the Cloudflare MCP).** `AppHeader` mounts `FeedbackWidget`; `useFeedbackSubmit.ts` posts the typed title + description, an optional screenshot and the recent console warn/error buffer to the Cloudflare Worker `feedback-worker` (outside this repo, shared with other apps via `APP_REPO_MAP`). The worker (1) **rejects any request without an allow-listed `Origin` header — native iOS / Android `fetch` sends none, so every native submission gets `403 Origin not allowed`** (reproduced with `curl`); (2) on web, sends the text to the Anthropic API for classification, uploads the screenshot to a *secret* (unlisted, not private) gist, and files a **GitHub issue** containing the verbatim text, the gist link and the session logs — and this repo is **public**. No `user-feedback` issue exists here yet. **Recommendation (owner to confirm): for v1.0 send feedback to Sentry User Feedback (`Sentry.captureFeedback`, text only) on every platform** — private, 90-day retention, already a disclosed processor in the policy and the privacy manifest, no Origin problem, ~1–2 hrs incl. tests; leave the shared worker alone. Fallback if there is no time: hide the widget in store builds and rely on the support email. Either way the App Privacy / Data Safety forms (#823, #2014) gain "Other user content — not linked, app functionality". Also found: the old `docs/privacy-policy.html` claimed nothing is stored server-side — wrong since scoring moved to the API; and backend Sentry events carry the `X-Session-ID` request header (not in sentry-sdk's scrub list).
- ⚠️ **Store privacy declarations were incomplete, and Blackjack affects the age rating (found Sep 20).** `PrivacyInfo.xcprivacy` and #2014's Data-safety checklist declared only Sentry's three diagnostics types; the app also collects a per-install session ID and gameplay records keyed to it, plus optional feedback text. #2442 completes the manifest (pinned by `privacyManifest.test.ts`) and adds paste-ready answers for both consoles in `docs/STORE-PRIVACY-ANSWERS.md` — **#2014's checklist is superseded by that file.** Two owner decisions in it: (1) session ID + gameplay data declared **"linked to the user"** on Apple's label (the conservative reading of a persistent per-install ID); (2) **Blackjack is simulated gambling** and will raise the age rating on both stores, possibly sharply in some regions — **run both rating questionnaires early** (well before the Oct 2 listing work); the fallback of hiding Blackjack via `gameVisibility.ts` leaves 5 free games and must precede screenshots (Oct 3).
- ⚠️ **The dev Render stack had not deployed since Aug 16 (found Sep 20 after #2436 merged).** Both dev services have **auto-deploy off**; they were deployed by `deploy.yml`, which is broken (next bullet), so merges to `dev` reached nobody: the live dev API was 156 commits behind (`2f00485b`) — no XP fields on `/stats/me`, no `/health/db` — while every TestFlight / Play test build talks to it. **API fixed Sep 20:** setting `ENVIRONMENT=development` over the Render MCP triggered a deploy of `dev` HEAD (`42a6109c`), live in ~2 min; no migrations were pending, only patch-level dependency bumps. Verified: `/health/db` → `{"status":"ok"}` (first run of that query on real Postgres, PG 18), `/stats/me` returns `arcade_xp` / `arcade_level` / `xp_into_level` / `xp_for_next_level`, dev entitlement override still on, and **Sentry shows the new deploy's transactions as `environment: development`, `release: 42a6109c…`** (pre-deploy events: `production`, release `2f00485b`) — #851's backend criterion is met. **Still stale: the dev web site `bc-arcade-frontend-dev` (Aug 16)** — low priority, left alone. **Until `deploy.yml` is repaired (#505), every backend merge to `dev` needs a manual deploy** (Render MCP `trigger_deploy` by service ID, or the dashboard) — PC: do this after the XP / daily-challenge backend PRs, or the frontend work will be tested against an old API.
- ❌ **`deploy.yml` is invalid at the workflow-file level** (found Sep 18): fails on every push to every branch, no successful run in its last 100. It calls `wcmchenry3-stack/gaming_app/.github/workflows/ci.yml@main` — apparently the repo's pre-rename name. Doesn't block PRs (push-event run, not a check) but **will block the Sep 28 prod deploy** — fix as part of #505.
- CI: `ci.yml` green on dev; iOS mobile smoke red since June (#2347) — deferred, manual TestFlight verification instead.
- ⚠️ **Correction (2026-09-18): Android Maestro smoke was never actually green.** Every "success" in `mobile-smoke-android.yml`'s history (including Sep 11) is the Maestro job being *skipped* because `detect-maestro-scope` found nothing relevant; every run where it really executed (~30, June → Aug 30, across `main` + 5 branches) failed or was cancelled — cold-launch ANR on the CI emulator plus an unbounded "Wait"-dismiss loop in `_shared/launch.yaml`. **Maestro is descoped from the launch on both platforms**: Android leg taken off `pull_request` (on-demand `workflow_dispatch` only), restoration tracked in #2400. Verification below relies on Jest + manual checks on real release builds instead.
- ❌ **Secret scanning was a no-op (found Sep 19).** `.gitleaks.toml` held only an `[allowlist]`; in gitleaks v8 a custom config *replaces* the built-in rules unless it sets `[extend] useDefault = true`, so `secret-scan` ran with zero rules (verified locally with gitleaks 8.30.1: a synthetic AWS-style key → "no leaks found"). Separately, the default `generic-api-key` rule cannot capture values containing symbols outside `[\w.=-]`, so it would have missed the old password anyway. Fixed in #2414 (defaults restored + a dedicated `UPLOAD_*_PASSWORD` rule; full `dev` history scans clean). Other repos sharing `called-secret-scan.yml` with an allowlist-only config have the same hole.
- ⚠️ **This PC could not run Gradle at all (found Sep 19).** Norton's HTTPS scanning re-signs TLS with a root the JDK doesn't trust, so every Maven/plugin download failed (`PKIX path building failed`, surfaced by Gradle as "plugin not found"). Fix: add `systemProp.javax.net.ssl.trustStoreType=Windows-ROOT` to the user-level `~/.gradle/gradle.properties` (the same file the keystore steps create), or exclude Java from Norton's SSL scanning. The PC also lacked NDK 27.0.12077973 (Gradle auto-installed it). With TLS worked around, `assembleDebug` then ran 38 min and **failed in `:react-native-audio-api:buildCMakeDebug` on the Windows 260-char path limit** (`ninja: error: mkdir(...)`; object paths reach ~335 chars from `C:/Users/<user>/claude_projects/BC-Arcade`, and the SDK's ninja ignores `LongPathsEnabled`). **Fixed Sep 19:** a short path does *not* work (`subst` is resolved back to the real path, and even `C:/bc` leaves 282-char object paths) — the fix is a long-path-aware ninja: 1.13.2 swapped into `Android/Sdk/cmake/3.22.1/bin` (original kept as `ninja-1.10.2.exe.bak`), after which `assembleDebug` and the release bundle task pass from the normal checkout. Both prerequisites are now documented in `docs/ANDROID-CI.md`. Still to do on the PC: make the TLS workaround permanent (`systemProp.javax.net.ssl.trustStoreType=Windows-ROOT` in the user-level `gradle.properties`) and prove one full signed `bundleRelease` before the first Play upload.
- Open PRs (Sep 20, late night) — **Mac, all draft, pulled forward:** **#2439** (in-app feedback → Sentry User Feedback; owner decision Sep 20), **#2441** (#1922 Privacy / ToS links in Settings), **#2442** (iOS privacy manifest completed + `docs/STORE-PRIVACY-ANSWERS.md` for #823 / #825 / #2014), **#2443** (backend Sentry scrubs `X-Session-ID` + `X-Admin-Token`), **#2444** (tile icon colour + tab bar corner background). CI: #2439 / #2441 / #2442 fully green; #2443 / #2444 still running at the time of writing. **PC:** **#2440** (XP frontend — Profile level card + Home level pill, closes #2391; draft, `/code-review medium` run), this progress log (**#2436** design D code and **#2437** legal drafts + ATT audit both merged Sep 20; so did the PC's #2433 (#2428 `FetchError` fix), #2434 (XP `/stats/me` wiring) and #2435 (PC plan log)), and release-please **#2424 "release 1.1.0"** (the regenerated #2332 — see below). Merged Sep 19–20: #2413 (#2403 fix), #2414 (keystore), #2416 (hide-premium), #2417 (pre-launch-API builds keep all 12 visible — so a TestFlight / test build from `dev` shows 12 games), #2418 (untrack iOS codegen), #2419 (XP `progression.py`), #2420 (Xcode Cloud test-hooks guard); off-plan, both hidden games: #2425 (Star Swarm PERFECT-clear fanfare hold) and #2427 (Sort bottle-row layout). #2332 was closed and release-please regenerated it as **#2424 ("release 1.1.0") — do not merge; hold until the Oct 6 version decision** (#857). #2374 and all Dependabot PRs merged; dev is at expo 57.0.23 / RN 0.86.3 / reanimated 4.5.5 / gesture-handler 3.3.0 / screens 4.28.0 / skia 2.11.2 / sentry-rn 8.26.0.

### Launch-gating issues (must close before submission)

| # | Issue | Notes |
|---|---|---|
| 1917 | Locked games blocker | ✅ Code complete Sep 20 — issue #2390: **#2416** (hide-premium), **#2417** (pre-launch-API builds keep all 12 visible) and **#2420** (Xcode Cloud test-hooks guard) all merged. Remaining: the mandatory store-configuration check (6 tiles, 3 tabs) on the Wed 30 prod-API builds. |
| — | **Pre-launch mode is off in what we submit** | No switch to remember — it follows the API URL — but prove it on the RC: (1) `ci_post_clone.sh` writes the **production** API URL (Tue 29; `gameVisibility.test.ts` fails on any third value); (2) RC build shows **exactly 6 tiles, 3 tabs**, no Ranks, on iOS and Android; (3) `curl` the production `/entitlements` with a fresh `X-Session-ID` → `entitled_games` is **empty** (`ENTITLEMENT_DEV_OVERRIDE` must not exist on the prod service — `test_render_yaml_prod_does_not_set_dev_override` covers `render.yaml`, not the Render dashboard, so check the dashboard too); (4) the prod API's `DATABASE_URL` host is `*.pooler.supabase.com` on port 5432 — **not** the Render dev DB (owner confirms in the dashboard; a prod play-through must add rows in Supabase and leave the Render DB's counts unchanged) |
| 1918/2277 | Keystore rotation + secret removal | ✅ **Done Sep 19, pending Google.** #2414 merged (passwords out of the repo, gitleaks repaired). The committed password did not open the old keystore and the real one was lost, so this became a **new upload key + Play Console upload-key reset** rather than a password change: new PKCS12 keystore outside every repo, signing values in the user-level `~/.gradle/gradle.properties`, `:app:signingReport` shows `release` signing with the new key (SHA-1 `F0:11:…:4F:D9`), reset request submitted with the new `.pem`. **Play uploads are blocked until Google approves the reset.** Owner follow-ups: off-machine backup of the `.jks` + password; delete the 7 unused `ANDROID_*` GitHub secrets; empty the OneDrive recycle bin; after approval delete the old keystore and the `.unknown-password` / `.stale` files. PC + Play Console; Sat 19 — procedure in the schedule row below |
| 1914 | 4.2 cohesion | XP (#2391) + daily challenge (#2392). XP backend: `progression.py` merged (#2419); `/stats/me` wiring merged (**#2434**, Sep 20). **XP frontend up Sep 20 — #2440** (two days early). Remaining for XP: owner review + merge, then the device check (pill on Home, +10 / +50 XP after a game, airplane mode → grid with no pill) on the next build. Daily challenge (#2392) is next |
| 828 | Privacy Policy + ToS hosted at stable URLs | buffingchi.com/privacy, /terms. **Drafts up Sep 20 — #2437** (`docs/privacy-policy.html` rewritten, new `docs/terms-of-service.html`, open owner decisions in `docs/LEGAL-REVIEW-NOTES.md`: feedback destination, publisher legal name + address, retention limit, governing law, liability cap). **Owner to-do, not blocking anything yet:** internal TestFlight / Play internal testing need no policy URL. Needed **by Fri Oct 2** (store listing work — both consoles require a live privacy-policy URL, as does external TestFlight) and hard-blocking at the **Oct 8 freeze / Oct 9 submit**. Remaining: owner + legal review, hosting, store + Settings wiring (#1922) |
| 1922 | Privacy/ToS links in Settings | **Code-complete Sep 20 — #2441** (one compact row under "Delete my data" — the screen does not scroll; 13 locales; URLs in `src/config/legal.ts`). Close when `buffingchi.com/privacy` and `/terms` resolve (#828) |
| 505 | Prod Render services (API + web) on `main` — **database is Supabase, not Render** (decision 5) | games-api.buffingchi.com + games.buffingchi.com. **Nothing exists yet** — create both services fresh (Oregon, starter), not from the blueprint as written (see ground truth). Full prod env-var set, all as dashboard secrets: `DATABASE_URL` (Supabase session pooler), **a new prod RS256 keypair** (`ENTITLEMENT_PRIVATE_KEY` / `_PUBLIC_KEY` — never reuse dev's), new `DAILY_WORD_SALT` / `DAILY_CHALLENGE_SALT`, new `ADMIN_API_TOKEN`, `SENTRY_DSN`, `ENVIRONMENT=production`, prod-only `ALLOWED_ORIGINS`, and **no `ENTITLEMENT_DEV_OVERRIDE`**. Cloudflare CNAMEs (owner). **A `dev` → `main` promotion PR must merge before the first prod deploy** — prod tracks `main`, which is behind. **Includes repairing `deploy.yml`** (stale `gaming_app` reusable-workflow reference — never succeeds today) |
| 2432 | **Supabase prod database** | New with decision 5. (1) Owner hardening in the dashboard — Mon 21; (2) schema + seeds via `alembic upgrade head` through the session pooler — Thu 24; (3) security advisors show no ERROR-level finding (Data API off — Alembic's `public` tables carry no RLS); (4) keep-alive: `GET /health/db` + an uptime monitor, so the free project never idles into a pause; (5) **upgrade to Pro and confirm daily backups before the Oct 8 freeze.** The pooler URL contains the DB password and this repo is public: it lives only in the password manager and the Render dashboard — never in the repo, a PR, an issue, chat, or an MCP tool argument |
| 823 | App Store Connect listing (metadata, screenshots, age rating) | Mac; screenshots after hide-premium lands |
| 825 | Play Console listing (assets, content rating, Data Safety) | PC |
| 2014 | Play Data Safety form (Sentry) | Manual, minutes |
| 830 | TestFlight internal track | Mac |
| 832 | Play testing tracks | PC |
| 836 | ATT/IDFA audit doc | **Drafted Sep 20 — `docs/ATT-AUDIT.md` in #2437: no ATT prompt needed.** Sentry Cocoa 9.28.0 device binary has zero IDFA symbols, ships its own no-tracking privacy manifest, and no target links `AdSupport` / `AppTrackingTransparency`. Remaining: owner sign-off on the attestation; bring `app.json`'s `privacyManifests` in line with the committed `PrivacyInfo.xcprivacy` post-launch |
| ~~2328~~ | iOS MessageQueue runtime error | ✅ Sep 18 — did not reproduce on a clean Metro cache + fresh native build (stale dev-client binary after the SDK 57/RN 0.86 bumps); recovery recipe in `docs/IOS.md` (#2404) |
| 2380 | Android offline errors spamming Sentry | ⚠️ #2402 merged Sep 18 but **incomplete** — it matches `CodedError`; Expo actually throws `FetchError extends Error` (Sep 20 Sentry audit). Finished by #2428 |
| 2403 | Offline retries/fallbacks ignore Android `CodedError` | ⚠️ **#2413 merged Sep 19, but the fix is incomplete — same `FetchError` gap; finished by #2428.** **Fixed Sep 19 — #2413 merged** (owner decision: fix without waiting for the device check; the airplane-mode Daily Word check becomes a confirmation on the Wed 23 Play internal build). Original note: candidate — decide after a device check. Found reviewing #2402: `withRetry.ts` never retries and `DailyWordScreen.tsx` never falls back to its cached puzzle when an Android offline failure arrives as `CodedError` instead of `TypeError` (3 more sites are premium/cosmetic). Both affect shipping free games. Test: Android build → airplane mode → open Daily Word. If the cached puzzle doesn't load, this gates launch (~1 hr: export `isNetworkError()` from `httpClient.ts`, use at all 5 sites) and goes ahead of hide-premium. |
| 2428 | **Offline `fetch` errors not recognised on device — Expo `FetchError`** (#2380 / #2403 follow-up) | Found Sep 20 (Sentry audit; BC_GAMES-4W / 4Y / 42). Expo 57's native `fetch` rethrows native failures as `FetchError extends Error` ("fetch failed: …"), which `isNetworkError()` (`TypeError` / `CodedError`) misses — so on iOS **and** Android an offline user still gets `errorType: unexpected` in Sentry, no `withRetry` retry, and no Daily Word cached-puzzle fallback (a free game). The #2402/#2413 tests used a synthetic `CodedError` that production never produces. Fix (~1 hr): extend `isNetworkError()` with a structural `fetch failed` message check and retest with the real error shape. **Mon 21 PC, ahead of XP.** Confirm with the airplane-mode Daily Word check on both platforms (Wed 23 builds); done when 4W / 4Y / 42 stop receiving events from builds that contain it. ⚠️ **Code done Sep 20 — #2433** (a day early): root cause confirmed in Expo's source (`expo/src/winter/fetch/FetchErrors.ts` — `FetchError extends Error`, message always `fetch failed: …`; `runtime.native.ts` installs it as the global `fetch`), matched by message prefix; the tests build the error from Expo's own class and **all 7 new cases failed on the old code first** — the check #2402 / #2413 never had. `/code-review medium`: no findings. The issue stays open (PR uses `Refs`, not `Closes`) until the two device checks pass |
| ~~2372/2374~~ | Hearts Android crash | ✅ Sep 18 — #2374 merged (game hidden but code ships) |
| ~~—~~ | iOS version string mismatch (1.0.0 vs 1.0.9) | ✅ Sep 19 — #2412 (Info.plist derives from the pbxproj build settings; Jest guard `nativeVersionSync.test.ts`). Confirm 1.0.9 on the next Xcode Cloud build |
| 851 | Sentry environments + backend release tag (**rescoped Sep 20: no separate prod DSN** — decision 5) | Today dev traffic is tagged `production` on both sides (see ground truth), so launch-day crash-free numbers would be meaningless. Fix = design D: frontend environment follows the API URL, backend reads `ENVIRONMENT`. Done when new dev-API events land under `development`, prod events under `production`, and backend events carry `release` = the deployed commit. Sentry MCP authenticated Sep 20 (audit in ground truth). Also absorbs **#2429** — no Sentry init / App Hang tracking on simulators and test-hooks builds. **Backend half verified Sep 20** (dev API events under `development` with the commit as `release`); frontend half waits for the next device build. Follow-up **#2443**: sentry-sdk's scrubber matches header names exactly and knew neither `X-Session-ID` nor **`X-Admin-Token`** — treat dev's admin token as possibly exposed to Sentry project members and rotate it when convenient (prod gets a new one, #505) |
| 857 | Versioning + forced-upgrade kill-switch | Descope to: pick launch version + document; kill-switch post-launch |

### Explicitly deferred post-launch

Maestro as a PR gate on either platform (#2400 Android, #2347 iOS — see the 2026-09-18 correction above), IAP (#822, #837–#842), Hearts/Yacht AI epics (#2283 umbrella, "one engine, degraded per difficulty"; #2233 Hearts and #2241 Yacht robustness, rescoped under it by the #2269 decision; #1596 closed — both games are premium and hidden in v1.0, so nothing here gates launch; revisit alongside IAP, and their AI approach is still documented in section E's per-game `CLAUDE.md`), i18n gaps (#2193–#2195, #2212 — free-game locales OK-ish; verify Daily Word #1924 lightly), a11y backlog (#2192, #2219, #2207–#2209, #854), CI hardening (#2347 iOS smoke, #2211 TS check, #2153), Sentry network-warning throttle (#2430 — pull into launch if trivial, like #2210), Percy epic, analytics epic (#1800), audio epic (#1779), drag polish (#2274), account deletion beyond what exists (#835 blocked by SSO #144), leaderboard/security mediums (#2215–#2217, #2210 — NOTE: #2210 rate-limit on /games/catalog is a small fix, pull into launch if trivial), all `chore`/`tech-debt` spikes.

## Feature designs (from code investigation)

### A. Hide premium games (~1.5–2 dev-days)

**Mechanism: compiled TS constant, NOT an env var and NOT server-driven.** Two reasons: (1) Apple 2.3.1 — the reviewed binary must equal what users get; a server flag that unhides content post-review is itself a rejection pattern; (2) **`frontend/ios/ci_scripts/ci_post_clone.sh` deletes `.env.production` and force-writes a 2-line `.env` on every Xcode Cloud build** — any new `EXPO_PUBLIC_*` var silently disappears, which could ship premium games to App Review by accident.

- New file `frontend/src/entitlements/gameVisibility.ts`: `HIDDEN_GAMES` set (the 6 premium slugs), `SHOW_HIDDEN_GAMES = __DEV__ || process.env.EXPO_PUBLIC_TEST_HOOKS === "1"` (reuses the existing test-build signal already set by the Maestro/Playwright test builds and used in `httpClient.ts`/`syncApi.ts`), `isGameVisible(slug)`. Keep visibility decoupled from `PREMIUM_GAMES` in `EntitlementContext.tsx` (unchanged — IAP re-activates it later).
- `frontend/src/screens/HomeScreen.tsx`: the 12-game array is a hardcoded literal (backend catalog is never fetched — leave it that way); rename to `ALL_GAMES` and filter through `isGameVisible`.
- `frontend/App.tsx`: conditionally register hidden games' screens in `LobbyStack()`; keep `makePremiumScreen()`/`LockedGameScreen` (still correct for dev/test builds). **Also hide the "Ranks" tab in `MainTabs()`** — `LeaderboardScreen.tsx` is StarSwarm-only, a dead tab once StarSwarm is hidden.
- Backend: **no change** (leave `is_active`/`is_premium` rows alone; `is_active=False` would break `POST /games` for internal testing).
- Maestro: **no work** (descoped 2026-09-18). Existing premium-game flows are unaffected because test builds set `EXPO_PUBLIC_TEST_HOOKS=1`; the `navigation.yaml` switch, the `home/hidden-games.yaml` negative flow, and the no-test-hooks CI job are dropped.
- Jest (now the only automated guard): verify `HomeScreen.test.tsx` behavior under Jest's `__DEV__`; add hidden-games describe block (copy the existing "Pachisi disabled" pattern) + `gameVisibility.test.ts`, which must assert all 6 premium slugs are hidden with `__DEV__ === false` and `EXPO_PUBLIC_TEST_HOOKS` unset.
- Manual guard: the **Wed 30 build #2 (production API)** and the RC are the real negative test on store infrastructure — exactly 6 tiles, 3 tabs, no locked screen reachable. (Was the Tue 22 / Wed 23 builds; those are pre-launch-API builds and now show all 12 by design.) Already proven locally Sep 19 on Release-configuration simulator builds of the same commit: dev API → 12 tiles / 4 tabs, production config → 6 tiles / 3 tabs, iPhone + iPad.
- Screenshots for stores must come from a no-test-hooks release build (correct 6-tile grid).

### B. Arcade XP + player level (~2 dev-days)

Pure derivation from the existing `games` table — no new table, no migration.
- New `backend/games/progression.py`: pure `compute_progression(summary: StatsSummary)` — base XP per completed game + variety bonus per distinct game played (breadth supports the anti-container narrative), `LEVEL_THRESHOLDS` array marked "tune post-launch". Unit tests with hand-built fixtures (`backend/tests/test_progression.py`).
- Extend `StatsResponse` in `backend/games/schemas.py` (`arcade_xp`, `arcade_level`, `xp_into_level`, `xp_for_next_level`); ~5-line change in `backend/stats/router.py::get_my_stats()`.
- Frontend: extend `StatsResponse` in `frontend/src/api/types.ts`; level header + progress bar on `ProfileScreen.tsx` (reuse bento styles); level pill on `HomeScreen.tsx` header via `statsApi.getMyStats()` + `withRetry` (omit badge on failure, never block the grid). i18n keys in `profile`/`common`.

### C. Daily cross-game challenge (~2.5–3 dev-days)

Clone the Daily Word stateless pattern — no new tables; completion is a read-side view over data already written by `PATCH /games/{id}/complete`.
- New `backend/daily_challenge/` package (mirrors `daily_word/`): `definitions.py` — deterministic template pick by `(YYYYMMDD + DAILY_CHALLENGE_SALT) % len(TEMPLATES)`; templates span free games only. **Only twenty48 gets `score_at_least` goals** (it's the only visible free game with comparable `final_score` — per migration 0003 comment); all others use binary "complete".
- `service.py`: one query over `games` join `game_types` for today's local-day window (client `tz_offset_minutes`, same as Daily Word); evaluate goals in Python.
- `router.py`: `GET /daily-challenge/today` (public, 60/min) + `GET /daily-challenge/status` (session-scoped via `get_session_id`, 60/min), mounted in `main.py`. Rate limits satisfy hard rule #12.
- Frontend: `frontend/src/game/daily_challenge/api.ts` (`createGameClient` pattern); `DailyChallengeCard.tsx` at top of Home ScrollView — goal chips with checkmarks, refetch on AppState foreground + network reconnect (`useNetwork()`); offline = lightweight "check connection" state, offline completions retroactively count when `scoreQueue` flushes. New `daily_challenge` i18n namespace.
- Tests: backend determinism + completion-detection; `DailyChallengeCard.test.tsx`; manual end-to-end play-through on sim/device (no Maestro flow — descoped 2026-09-18).

**Top design risks:** (1) env-var regression re-introducing the Xcode Cloud clobber gap — call out in PR; (2) with Maestro descoped there is no automated check of a no-test-hooks build — the Wed 30 production-API builds and the RC check are mandatory, not optional (the Tue 22 / Wed 23 builds are pre-launch-API builds and show all 12 by design); (3) don't bikeshed XP constants pre-launch; (4) richer challenge goals are out of scope; (5) verify Jest `__DEV__` assumption empirically.

### D. Prod data + observability (~1 dev-day code, ~0.5 day ops) — added 2026-09-20

Topology after this lands: **prod** = Render API + static site on `main` → **Supabase** Postgres; **dev** = Render API + static site on `dev` → Render `bc-arcade-db`; **local + CI** = SQLite via `tests/conftest.py`. One code PR (`chore/prod-readiness`, Thu 24), then ops.

- `render.yaml`: relabel the `databases:` block as **dev**; prod API `DATABASE_URL` → `sync: false` (comment: Supabase session pooler, port 5432); add `ENVIRONMENT` (`production` / `development`) to both APIs; declare the secrets the backend reads but the yaml omits (`ENTITLEMENT_PRIVATE_KEY`, `ENTITLEMENT_PUBLIC_KEY`, `DAILY_WORD_SALT`, `ADMIN_API_TOKEN`) as `sync: false` on both.
- `backend/tests/test_entitlements.py`: beside `test_render_yaml_prod_does_not_set_dev_override`, add a guard that the prod API's `DATABASE_URL` carries **no `fromDatabase`** — so the dev-DB wiring cannot come back.
- `backend/main.py`: `sentry_sdk.init(...)` gains `environment=os.environ.get("ENVIRONMENT", "development")`, `release=os.environ.get("RENDER_GIT_COMMIT")`, `send_default_pii=False`; new rate-limited `GET /health/db` (`SELECT 1`, reusing the startup connectivity check) for the keep-alive monitor. Rate limit satisfies hard rule #12.
- `frontend/App.tsx`: Sentry `environment` = explicit `EXPO_PUBLIC_SENTRY_ENVIRONMENT` if set, else `isPreLaunchApiBuild()` (`src/game/_shared/envFlags`) ? `"development"` : `"production"`. Same rule as #2417 — **it follows the API URL, so there is no switch to remember** and the `ci_post_clone.sh` env clobber cannot break it: the Tue 29 URL change flips game visibility, entitlements _and_ the Sentry environment in one step. Jest test beside `gameVisibility.test.ts`. **#2429 rides in the same PR:** also skip `Sentry.init` when `EXPO_PUBLIC_TEST_HOOKS=1` (the `httpClient` `captureException` path is not test-build gated today), and turn off `enableAppHangTracking` for simulators / non-production builds — simulator App Hangs (BC_GAMES-4X / 4Z) and CI-emulator events were landing in `production`.
- Docs: rewrite `docs/RENDER.md` (it still describes the `gaming_app` repo, "no database" and a single environment) — two-environment topology, Supabase connection rules, full env-var table; one DB-topology paragraph in `ARCHITECTURE.md`.
- Ops after merge: set `ENVIRONMENT=development` on the live dev API (Render MCP `update_environment_variables`); `alembic upgrade head` against Supabase from the Mac (later migrations apply themselves — the prod API's `startCommand` runs Alembic on every boot, same as dev).
- **Explicitly not doing pre-launch:** Supabase CLI / branching, a second Supabase project for staging, a least-privilege DB role (the app connects as `postgres`), `verify-full` TLS with Supabase's CA, Sentry alert rules. All post-launch hardening.

**Design risks:** (1) PG 17 (Supabase) vs PG 18 (dev) — the schema is vanilla (no extensions; JSONB, UUID, timestamptz), but the test suite only ever runs on SQLite, so the Thu 24 `alembic upgrade head` is the first real proof; (2) session-pooler connection ceiling on the free tier vs the app's pool of 5 + 5 overflow plus one Alembic connection at boot — fine for one instance, revisit before scaling out; (3) a free-plan pause during App Review is a prod outage — hence the keep-alive now and Pro before submit; (4) the pooler URL leaking into this public repo — gitleaks will not reliably catch a Postgres URL, so it never goes anywhere but the password manager and the Render dashboard.

### E. Closing task — document every game + full architecture review (after submission) — added 2026-09-20

Owner request (Sep 20): the last thing this plan does is document all 12 games and how the app works, in `CLAUDE.md` form, together with a full architecture review. Scheduled into the **Sat 10 – Thu 15 review-wait window**: no code freeze to fight, and the architecture is at its launch shape.

- **Per-game `CLAUDE.md`** in each `frontend/src/game/<name>/` (12 games + `_shared/`): rules as implemented, engine state shape and entry points (`engine.ts`), storage/persistence keys, scoring and what the server records, offline behaviour (`scoreQueue` / `SyncWorker`), AI approach where one exists (hearts, yacht), premium/visibility status, test files, known gotchas. Nested `CLAUDE.md` files load only when a session works in that directory — full detail at no cost to every other session. Matching `backend/<package>/CLAUDE.md` where a game has server-side logic (`daily_word`, `daily_challenge`, `games`, `stats`, entitlements).
- **Root `CLAUDE.md` stays a tight index (< 50 lines)** — it gains one line pointing at the per-game files, nothing more.
- **Full architecture review** → refresh `docs/ARCHITECTURE.md` end-to-end against the code as shipped (client engines, sync/offline queue, entitlements + visibility, progression, daily challenge, DB topology from D, observability, CI/release paths). **Includes a new "Infrastructure & external services" section** — none exists today (`ARCHITECTURE.md` and `RENDER.md` have no Cloudflare content at all): one map of what each service is for and how they connect — **GitHub** (Actions, CodeQL, gitleaks, release-please, Dependabot, branch protection), **Render** (dev + prod API and static sites, dev Postgres), **Supabase** (prod Postgres, session pooler, Pro/backups), **Sentry** (org / projects / environments / release tagging, the triage routine above), **Cloudflare** (DNS for `games-api` / `games` / buffingchi.com, proxy and TLS mode, any Workers / Web Analytics — gathered read-only over the Cloudflare MCP at that time, not from memory), plus the store toolchains (Xcode Cloud → App Store Connect, Gradle → Play Console). Per service: what it hosts, who owns the account, which secrets live there (names only — never values), and the failure mode if it goes down. Findings become GitHub issues (`tech-debt` / `security`), not inline fixes — the launch branch stays frozen.
- **How (token-aware):** one `haiku` Explore agent per game, in parallel, each writing a draft from a fixed template; `sonnet` for the four engines with real algorithmic depth (hearts, yacht, sudoku, mahjong); the main session writes the cross-cutting architecture review itself and spot-checks every draft against the engine code before merge. One docs PR per ~4 games to keep reviews readable.

## Day-by-day schedule (Sep 18 → Oct 15)

_Weekday labels corrected 2026-09-19 — they were one day early throughout (Sep 18 2026 is a Friday, submit day Oct 9 a Friday, Oct 15 a Thursday). Dates never moved._

Both machines available daily. Mac = Xcode Cloud/TestFlight/iOS sim/ASC; PC = Gradle/Play Console/backend + most feature dev. Feature code (frontend/backend) is machine-agnostic — the split below assigns it wherever the day's platform work already is. All work branches from `dev`, lands via draft PRs per the git standard.

### Week 1 — clear the decks + hide premium (Sep 18–24)
| Day | PC | Mac |
|---|---|---|
| **Fri 18** ✅ | Done: #2374 merged; #2401 Maestro descope merged; #2380 fixed early (#2402); #2332 decided (hold to Oct 6); implementation issues filed (#2390 hide-premium, #2391 XP, #2392 daily challenge — no separate umbrella, epic #1916 + the gating table above serve that role; gating checklist posted as a task-list comment on #1916); #2400 filed (Maestro restoration, post-launch); #2403 filed (offline `CodedError` follow-up — launch candidate); keystore investigated and Sat 19 procedure worked out (see ground truth). Found: `deploy.yml` broken (see ground truth / #505). Still open: Play Console record + testing-track history check; airplane-mode Daily Word device check for #2403. | Done: Dependabot cleared (#2396 expo 57.0.23, #2395 12-pkg group — each needed a `pod install` Podfile.lock commit pushed onto the Dependabot branch); #2328 closed (#2404). Evening: owner ran an Xcode Cloud → TestFlight build of dev with all of the day's merges in (SDK 57 / RN 0.86 bumps included) — it built, uploaded and processed, which closes the "Xcode Cloud build of current dev" item and proves the ASC app record + bundle ID. |
| **Sat 19** | **Status (evening):** afternoon went to an off-plan Star Swarm regression fix (#2409/#2410 → #2411, merged — a hidden game in v1.0). Then: keystore PR **#2414** merged (passwords stripped, docs fixed, gitleaks repaired — see ground truth) and the owner rotated to a **new upload key** the same evening (Play upload-key reset pending Google — see the gating table; the numbered steps below are the original plan, superseded); #2403 fixed without waiting for the device check (**#2413**, merged); hide-premium started a day early (branch `feat/hide-premium-v1`: `gameVisibility.ts`, Home filter, conditional routes + Ranks tab, `gameVisibility.test.ts`). Hide-premium review triage (9 findings; TEST_HOOKS-leak guard for `bundleRelease` is the one that must land before the Wed 23 Play build) is on #2390. Still open: owner keystore steps; Play Console record check. **#1918/#2277 keystore** (owner, ~20 min, no Google round-trip): (1) new letters+digits password into the password manager; (2) `keytool -importkeystore … -deststoretype PKCS12 -srcalias upload` into a fixed location **outside every checkout** — non-destructive, original stays as fallback (replaces the earlier `-storepasswd` idea, which rewrites in place); (3) `keytool -list -v` → compare SHA-1 with Play Console → App integrity → Upload key certificate, and confirm Play App Signing is enrolled; (4) user-level `~/.gradle/gradle.properties` gets `UPLOAD_STORE_FILE` (absolute, forward slashes) + both passwords — overrides the tracked file, so every checkout/worktree signs from one keystore with no `build.gradle` change; (5) `./gradlew :app:signingReport` proves it; (6) delete the old-password original, back up the new file off-machine; (7) refresh the 3 `ANDROID_UPLOAD_*` secrets, delete the 4 legacy ones. **Then Claude's PR**: strip lines 72 + 74 from tracked `gradle.properties`, fix `docs/ANDROID-CI.md` (wrongly says passwords come from env vars), find out why gitleaks never flagged it, verify `assembleDebug` + CI release-smoke, close both issues. Order matters: steps 1–5 before pulling that PR. Carry-over: Play Console record check; #2403 device check. | ✅ **iOS version string fixed — #2412 merged.** Root cause: `version-sync.yml` patches the pbxproj but never Info.plist, so the plist's literals sat at 1.0.0 / 1. Info.plist now references `$(MARKETING_VERSION)` / `$(CURRENT_PROJECT_VERSION)`; Jest guard `nativeVersionSync.test.ts`; `docs/IOS.md` §Version numbers (incl. "never upload a local archive"). Verified on a Debug simulator build (built app reports 1.0.9 / 10009); CI's `ios-build-check` skips without a native package bump, so **the 1.0.9 confirmation rides on the Tue 22 TestFlight build**. #2412 also carried a one-line ruff UP035 fix in `games/ranking.py` — ruff 0.16.7 (pinned by #2394) made the local lint gate block every PR from an up-to-date venv. Carry-over closed by the Sep 18 TestFlight build (see row above); remaining ASC item is a 30-second glance for a pending-agreement banner (Business → Agreements) — fold into the Oct 2 listing work. **Mac gotchas found:** (1) 73 files under `frontend/ios/build/generated/` are tracked despite `frontend/ios/.gitignore` ignoring `build/`, and the committed copies predate RN 0.86.3 — a checkout that restores them breaks local iOS builds (`no type named 'ResultT'` in `safeareacontext.h`); `pod install` regenerates them and dirties the tree. Xcode Cloud unaffected (`ci_post_clone.sh` runs `pod install`). Follow-up: `git rm -r --cached frontend/ios/build`, same pattern as #2304. (2) After a backend Dependabot merge, re-run `pip install -r requirements.txt -r requirements-dev.txt` — a stale venv (anyio 4.13.0 vs the pinned 4.15.1) fails the pre-push pip-audit gate. (3) `.release-please-manifest.json` says 1.0.8 while the app is 1.0.9 — input for the Oct 6 version decision. |
| **Sun 20** ✅ | Done (worked the evening of Sep 19): hide-premium (A) implemented and **merged as #2416** — `gameVisibility.ts`, Home grid + prefetch filter, premium routes and the Ranks tab registered from one tested registry (`premiumRoutes.ts`), Profile re-derived from visible games (+ game names localised instead of raw slugs), Jest store-build coverage (full suite green: 3,214 tests), `ARCHITECTURE.md` §10.7. Two Fable `/code-review high` passes drove two additions: **a Gradle guard that refuses any release build when `EXPO_PUBLIC_TEST_HOOKS=1` leaks in, and Metro's cache keyed on `EXPO_PUBLIC_*` values (`metro.config.js`) — reproduced the bug first: a release bundle built after a test-hooks bundle kept the hooks on via Metro's stale transform cache, on any platform.** Owner decision (Sep 19): the Render web build hides the games too. Left for the Mac: a 3-line `EXPO_PUBLIC_TEST_HOOKS` check in `ci_post_clone.sh` (Xcode Cloud's own env vars are not checked today). **PC can now build Android** (ninja 1.13.2 swapped into the SDK's cmake dir; `assembleDebug` + release bundle task pass). **Sentry triage (evening, MCP now authenticated):** 10 unresolved issues audited → #2428 / #2429 / #2430 filed (BC Arcade) and #667 / #668 (RulersAI); BC_GAMES-4X / 4Z / 2F resolved — see ground truth. | ✅ **Done Sep 19 (a day early).** A Debug build can't test this (`__DEV__` shows everything), so: two **Release-configuration** simulator builds of one commit, no test hooks — dev API (`.env.production` removed, as Xcode Cloud does) → 12 tiles / 4 tabs, every tile PLAY; production config → **exactly 6 tiles / 3 tabs**, Ranks absent. iPad mini: clean 2×3 grid. All six free games launch (Daily Word shows "Could not load today's puzzle" on the production-config build only because the prod API doesn't exist yet, #505 — loads fine against dev). Leak review of #2416 for store builds: no blockers (posted on #2390). **Owner decision → #2417** (pre-launch-API builds keep all 12 visible; see decision 1). Also: **#2418 merged** (untracked `frontend/ios/build/generated/` — the stale committed codegen was what broke local iOS builds; `docs/IOS.md` told people to restore it), **#2420 merged** (the `ci_post_clone.sh` test-hooks guard the PC left for the Mac — first version silently passed because `grep` exits 2 when a listed file is missing; fixed and tested under `sh`; documented in `docs/IOS.md`), **#2419 merged** (XP `progression.py` + tests, Mon 21's Mac item). **CodeQL:** #2418 removed the repo's only C-family sources (69 of 70 were that generated codegen), so the default-setup `Analyze (c-cpp)` job began failing every PR with "No source files found" — not a required check, but red everywhere; owner approved removing `c-cpp` from the code-scanning languages (now actions / javascript-typescript / python). A PR opened before the change keeps a stale red `c-cpp` result until its next push. Gotchas: this Mac is **Intel** — command-line simulator builds need `ARCHS=x86_64`; an arm64 build compiles then won't install; a local Release bundle loads the tracked `.env.production` (production URL). Cosmetic, pre-existing: dark-theme tab bar shows white corners behind its rounded panel on iOS 26; Solitaire + FreeCell tile icons are black-on-dark — fix before store screenshots (Sat 3). **Both addressed Sep 20 in #2444:** the icons are text-presentation glyphs (`♠`, `🂡`) drawn in an unset — black — Text colour, now `colors.text`; the tab bar gets a backing view painting `colors.background` behind its corner cut-outs. **The tab bar fix is reasoned, not yet seen on a device — check Home in dark theme on the next TestFlight build.** |
| **Mon 21** | ✅ **Both items done the night of Sep 20 (a day early): #2433 (#2428) and #2434 (XP `/stats/me` wiring — 627 backend tests, 96% coverage).** Sentry triage at session start: 3 unresolved in `bc_games` — 3N (#2430), 4W / 4Y (#2428) — nothing new; 42 no longer listed. PC gotcha: two Yacht `optimal*` "all-zero mock table" Jest cases time out under a full-suite parallel run on this PC and pass in isolation — not a regression. **Owner, still open on the PC:** add `systemProp.javax.net.ssl.trustStoreType=Windows-ROOT` to the user-level `~/.gradle/gradle.properties` (verified missing by match count — Claude does not open that file); Play Console record + testing-track check. **Mon 21 is now free on the PC → start Tue 22's XP frontend on a new branch `feat/arcade-xp-frontend` (#2434 merged, so `feat/arcade-xp` is gone).** Original cell: _(#2416 already merged Sep 20.)_ **#2428 first (~1 hr, launch-gating — `FetchError` in `isNetworkError()`, see the table).** **XP (B) backend moves here from the Mac column** (the old "finish A" day is free): ~~`progression.py` + tests~~ — **already done on the Mac and merged (#2419): build on `games/progression.py`, don't rewrite** — then schemas/router wiring, backend tests green. | ✅ XP `progression.py` + tests done Sep 19 (**#2419**). **Cut TestFlight internal build #1 today** (pulled forward from Tue 22) — **#2416, #2417 and #2420 are all merged**, so nothing blocks it: confirm **all 12 games visible and free**, **version shows 1.0.9** (first Xcode Cloud build after #2412 — new TestFlight version train; earlier uploads were 1.0.0), and `=== test-hooks guard passed ===` in the post-clone log. If the merges slip, the build slips with them — not on the critical path. **Owner, ~15 min, Supabase dashboard (either machine — decision 5 / design D):** rename the project `bc-arcade-prod`; reset the DB password into the password manager; turn on **Enforce SSL**; turn **off the Data API** (the app never uses PostgREST, and Alembic's `public` tables have no RLS — _this does not affect Claude: the Supabase MCP runs over the Management API, not the Data API; prove it with `get_project` + `list_tables` right after the toggle_); leave the GitHub integration inert. ✅ **Owner hardening done Sep 20; verified from the Mac the same evening without any secret:** `get_project` → `bc-arcade-prod`, `ACTIVE_HEALTHY`, PG 17.6; **Enforce SSL on** — `psql "host=aws-0-us-west-2.pooler.supabase.com port=5432 user=postgres.<ref> sslmode=disable"` with a bogus password is refused with `ESSLREQUIRED` before authentication (that host is this project's session pooler; `aws-1-…` answers `tenant not found`); the only non-SSL backend is Supabase's own `postgres_exporter` on localhost; **Data API off** — a request with the (public) publishable key to `/rest/v1/<table>` returns `503 PGRST002` and `/graphql/v1` returns 503; `get_advisors` (security) → no findings; `public` is empty until Alembic runs. The MCP kept working throughout (Management API). Cannot be verified from outside: that the new DB password is in the password manager. **Re-check after the Thu 24 `alembic upgrade head`:** the same keyed `/rest/v1/alembic_version` request must still return no rows, and `get_advisors` must show no ERROR-level "RLS disabled in public" finding. Then log the **Sentry MCP** in (interactive — `!`-prefixed in a Claude session) so #851 can be audited. |
| **Tue 22** | ✅ **Done the night of Sep 20 (two days early) — #2440.** `LevelProgress` card on Profile (real `progressbar` for screen readers; max level = `xp_for_next_level` 0), level pill in the Home header via `AppHeader.rightSlot` (fetch on mount / focus / back-online; never blocks the grid; **skipped while known offline** so it adds no #2430-style Sentry noise), i18n (`profile` is an English-only namespace; the two `common` keys translated in-session into all 13 locales — no `translate.js`, no OpenAI key). Jest 3,279 green, `tsc` unchanged at 178 (#2211). Same session: resolved this PR's conflict with #2435 (both machines had logged Sep 20 on the same lines — **whoever logs second should rebase the plan branch first**); Sentry triage: still only 3N / 4W / 4Y. **PC is now two days ahead → next PC session starts Wed 23's daily-challenge (C) backend design on Fable.** Original cell: XP frontend: types, ProfileScreen level header, Home level pill, i18n. _(One day earlier than planned, as is every PC row below through Sat 26.)_ | _(TestFlight build #1 moved to Mon 21; its "premium tiles absent" check moved to Wed 30 — see decision 1.)_ ✅ **Done Sep 20 (two days early) — #2437.** Pulled forward from Mon 28: draft Privacy Policy + ToS. The haiku draft needed a main-session fact-check against the code and the policy body was rewritten — the April policy (and the first draft) described an app that stores nothing server-side; see the ground-truth feedback-widget bullet and `docs/LEGAL-REVIEW-NOTES.md` for the owner decisions that block final text. **Lesson for section E:** haiku drafts of anything factual need every claim checked — this one also got Apple's EULA refund clause backwards. |
| **Wed 23** | XP PR review + merge. Start daily challenge (C) backend package. **Play internal upload only if Google has approved the upload-key reset** (see ground truth) — if still pending, install the locally signed release build on a device instead and upload when approved; not on the Oct 9 critical path. Per the amended decision 1 this is a pre-launch build: bundle it against the dev API and expect **all 12 games** (see the Mac cell); the "6 tiles, 3 tabs" store check happens on the Wed 30 prod-API build. | Upload same rev to **Play internal track**; confirm install on Android device. **To show all 12 it must be bundled against the dev API** — a plain Gradle release build loads the tracked `.env.production` (production URL), which makes it a 6-tile store build with no backend until #505. |
| **Thu 24** | C backend: definitions/service/router + rate limits + tests. | Buffer: any fallout from first builds (Xcode Cloud, signing, Sentry symbols). ✅ **#836 ATT audit done Sep 20 (in #2437)** — no ATT needed. Gotcha: `Pods/sentry-xcframeworks/<ver>/Sentry.xcframework` is a symlink into `~/Library/Caches/sentry-react-native/`, so a `grep -r` over `Pods/` silently searches nothing; inspect the binary there (`grep -a`, `otool -L`). ✅ **Design D code PR opened Sep 20 — #2436** (draft; backend 635 tests / 96%, Jest 3,253 green): `render.yaml` fix + two guard tests, Sentry environments both sides (`frontend/src/utils/sentryConfig.ts`; debug builds are always `development`), `/health/db`, #2429 (no `Sentry.init` on test-hooks builds, App Hang tracking off in debug builds, simulator events dropped from `production`), `docs/RENDER.md` rewrite, `ARCHITECTURE.md` §11. **Merged Sep 20; post-merge ops done the same night** — `ENVIRONMENT=development` set on the dev API, deploy verified, Sentry shows `development` + the commit as `release` (see the ground-truth bullet on the stale dev stack). A `/code-review medium` pass caught that the tracked `frontend/.env.production` / `.env` pinned `EXPO_PUBLIC_SENTRY_ENVIRONMENT`, which would have overridden the API-URL rule in Gradle and Render builds — removed, guarded by `sentryEnvFiles.test.ts`. Frontend half of #851 still to confirm on the next TestFlight / Play build (events under `development`). Then **`alembic upgrade head` against the Supabase session pooler** from this Mac (owner supplies the URL via the shell environment, never in chat) — builds the prod schema and resets the free plan's idle clock before Sep 27. Verify with the Supabase MCP: tables present, 12 `game_types`, `alembic_version` = dev head, security advisors clean. |

### Week 2 — daily challenge + prod env + legal (Sep 25–Oct 1)
| Day | PC | Mac |
|---|---|---|
| **Fri 25** | C backend polish; salt env var on Render. | C frontend start: `api.ts`, `DailyChallengeCard` skeleton. |
| **Sat 26** | C: Jest (`DailyChallengeCard.test.tsx`); PR review + merge. | C frontend: card UI, offline state, refetch wiring, i18n. |
| **Sun 27** | **PC buffer day** (gained by finishing A a day early). Spill-over from XP/C. **Pull #505 prep forward (now firm, not optional — Mon 28 is full):** repair `deploy.yml`; open the `dev` → `main` promotion PR so it is reviewed and green before Mon 28. | Manual play-through of challenge completion end-to-end on sim/device. |
| **Mon 28** | **#505 prod Render services** (DB already exists on Supabase — Thu 24): merge the `dev` → `main` promotion PR; create the prod API + static site (Render MCP, by hand — not the blueprint), pinned to `main`; **owner pastes the secrets in the Render dashboard** (Supabase session-pooler `DATABASE_URL`, new RS256 keypair, salts, `ADMIN_API_TOKEN`, `SENTRY_DSN`, `ENVIRONMENT=production`); Cloudflare CNAMEs for games-api / games (owner); first deploy — Alembic on boot should be a no-op; point an uptime monitor at `/health/db`. **#851** verify Sentry environments (dev → `development`, prod → `production`, backend `release` set). ZAP scan vs prod API (deployment standard). | _(Privacy Policy + ToS draft pulled forward to Tue 22.)_ Fold in the owner's review comments so the text is final before it is hosted Tue 29. |
| **Tue 29** | Host privacy/terms at buffingchi.com; **#1922** Settings links (i18n'd). | Point release config at prod API (`ci_post_clone.sh` `EXPO_PUBLIC_API_URL`) — **this is also the step that hides the six premium games and ends the free entitlements** (decision 1); verify entitlements/leaderboards/daily endpoints against prod, including that `/entitlements` grants a fresh session **nothing**. The same URL change flips the app's **Sentry environment to `production`** (design D) — confirm the first prod-API build's events land there. |
| **Wed 30** | **Play closed-track build #2** (all features, prod API). **Mandatory store-configuration check: exactly 6 tiles, 3 tabs.** | **TestFlight build #2** (all features, prod API). **Mandatory store-configuration check: exactly 6 tiles, 3 tabs, no Ranks, no locked screen reachable; Daily Word loads against prod.** |
| **Thu 1** | Device QA Android: all 6 free games, challenge, XP, offline queue. Log bugs. | Device QA iOS: same script + VoiceOver sanity on Home/Settings. |

### Week 3 — listings, screenshots, RC (Oct 2–8)
| Day | PC | Mac |
|---|---|---|
| **Fri 2** | **#825** Play listing: content rating questionnaire, **#2014** Data Safety (Sentry crash/diagnostics), store description. | **#823** ASC listing: metadata, age rating, privacy-policy URL, App Privacy answers (match PrivacyInfo.xcprivacy). |
| **Sat 3** | Android screenshots (release build, no test hooks) — phone + 7"/10" tablet if targeted. | iOS screenshots — 6.7"/6.1" (+ iPad since `supportsTablet: true`). |
| **Sun 4** | Fix QA bugs from Oct 1. | _(#836 ATT audit doc pulled forward to Thu 24.)_ Draft **reviewer notes** (XP + daily challenge walkthrough, per #1914 Tier 1 item 5). |
| **Mon 5** | Bug fixes continued; Sentry crash-free monitoring on both tracks. | Same; re-test fixed areas. |
| **Tue 6** | Version decision: land release-please 1.1.0 (or bump to it) — one version across app.json/build.gradle/`project.pbxproj` (all patched by `version-sync.yml`; **do not edit Info.plist** — it derives from the pbxproj); release notes. | Verify Xcode Cloud picks up version cleanly. |
| **Wed 7** | **RC builds both platforms**; manual regression script on the RC build (all 6 free games, challenge, XP, offline queue, Settings links). | Same, iOS side. |
| **Thu 8** | Final sweep of #1916 checklist + this plan's gating table; freeze except showstoppers. **Supabase on Pro (owner, billing) with daily backups listed** — gating, decision 5. | TestFlight external/internal sanity pass on RC. |

### Week 4 — submit + buffer (Oct 9–15)
| Day | Action |
|---|---|
| **Fri 9** | **Submit both stores** (6 days of buffer ahead of the deadline): ASC "Submit for Review" with reviewer notes; Play production (or open testing → production per account state) with release notes. |
| **Sat 10 – Thu 15** | Monitor review status + Sentry. If rejected: same-day triage, fix, resubmit — buffer absorbs 1–2 rejection round-trips (most likely: 4.2 narrative or metadata nits). If approved early: staged rollout on Play (10–20%), hold iOS release for manual release if desired. **In parallel — the plan's closing task (section E):** per-game `CLAUDE.md` files for all 12 games + `_shared/`, and the full architecture review into `docs/ARCHITECTURE.md`. Docs only; review findings are filed as issues, not fixed on the frozen branch. |

## Execution conventions

- **Branches/PRs**: one branch per workstream (`feat/hide-premium-v1`, `feat/arcade-xp`, `feat/daily-challenge`, `fix/…`), draft PRs → dev, user merges. Never push to dev/main.
- **Models**: see "Model + token usage policy" at the end of this document.
- **Sentry triage — at the start of every session that works this plan, and again before each store submission.** Main session, over the Sentry MCP (org `buffing-chi`, region `https://us.sentry.io`, project `bc_games` — by slug, never list): `search_issues` `is:unresolved`, sorted by `freq`, `limit` ~20, 14–30d. For each issue: (1) already tracked by a gating-table row or GitHub issue → note it, leave it open; (2) new and real → `get_sentry_resource`, find the cause in code, file a GitHub issue (with an iOS / Android label — ask which platform if the event doesn't say, per `CLAUDE.md`) and add a row here if it could gate launch; (3) simulator / CI / dev noise, or stale → resolve with a `reason` linking the tracking issue; (4) a fix has shipped → resolve only once the next build's events stop. From Mon 29 (first prod-API build) also filter `environment:production` — the only environment whose crash-free number counts. Record what was found in that day's log. The `rulersai` / `bc_bookshelf` projects belong to other repos: file there, keep them out of this plan.
- **Agent tooling gotchas (PC, Sep 18)**: the project agents in `.claude/agents/` (`plan-issues`, `lint-review`, `policy-compliance`) don't register as agent types — the files lack `name`/`description` YAML frontmatter; use a general-purpose haiku agent with the same brief until fixed. Agent worktree isolation fails on this PC over a path-casing mismatch (`bc-arcade` vs `BC-Arcade`) — create the worktree manually and point the agent at it. Any PR opened before #2401 merged carries a stale Maestro run: cancel it and merge `dev` in.
- **Manual/user-only tasks** (Claude can't do these): Play Console + ASC console actions (keystore upload, Data Safety form, content rating, screenshots upload, submit buttons), DNS/hosting for buffingchi.com legal pages, Apple/Google account agreements. **Added Sep 20 (design D):** Supabase dashboard settings, DB password and the Pro upgrade; generating the prod RS256 keypair / salts / admin token and **pasting every secret into the Render dashboard** (Claude creates the services and non-secret env vars over MCP, but secrets never pass through a Claude session); Cloudflare CNAMEs for games-api / games; the Sentry MCP login; the uptime-monitor account.

## Verification

- Hide-premium: a release-mode build (no `EXPO_PUBLIC_TEST_HOOKS`) compiled against the **production** API shows exactly 6 tiles, 3 tabs, no locked screens reachable — checked by hand on the Wed 30 build #2 and on the RC, both platforms. Pre-launch-API builds (Mon 21 TestFlight #1, Wed 23 Play internal) show all 12 by design (decision 1, #2417). `gameVisibility.test.ts` + `premiumRoutes.test.ts` green.
- XP/challenge: backend tests green (`python -m pytest tests/ -v`, 80% floor holds); play a game → XP rises on Profile; complete a challenge goal → checkmark on Home; offline completion syncs and retroactively satisfies goal.
- Keystore: fresh clone contains no secrets (`git grep -i password frontend/android`); `:app:signingReport` SHA-1 matches Play Console's upload certificate; release build signs from the user-level `~/.gradle/gradle.properties` (no keystore or password inside any checkout); ZAP scan on prod API returns zero high-severity.
- Prod database (Thu 24): Supabase MCP `list_tables` shows the Alembic tables; `select count(*) from game_types` = 12; `select version_num from alembic_version` = dev's head; `get_advisors` (security) has no ERROR-level finding; backend tests green including the new `render.yaml` no-`fromDatabase` guard; Jest green including the Sentry-environment test.
- Prod stack (Mon 28): `curl https://games-api.buffingchi.com/health/db` → ok; `/entitlements` with a fresh `X-Session-ID` → empty `entitled_games`; Render `get_service` shows branch `main`; a prod play-through adds rows in Supabase while the Render dev DB's row counts do not move (`query_render_postgres`); Sentry shows new dev-API events under `development`, prod under `production`, backend events with a `release`.
- Supabase plan (Thu 8): `get_organization` reports `pro`; daily backups listed in the dashboard.
- Sentry (every session; again at the Thu 8 freeze and before each submit): the triage in "Execution conventions" done and logged; no unresolved, untracked `BC_GAMES-*` issue in `production`; BC_GAMES-4W / 4Y / 42 have stopped receiving events from builds containing #2428.
- Store readiness: every row of the launch-gating table above closed or explicitly waived before Oct 8 freeze; crash-free > 99% on both test tracks before submission (#821 success metric) — **measured on Sentry's `production` environment from the first prod-API release onward** (earlier `production` data is mis-tagged dev traffic).

## Model + token usage policy

- **Sub-agents:** `haiku` for mechanical work (exploration, lint fixes, translation file passes, doc drafting, issue filing bodies, per-game `CLAUDE.md` drafts); `sonnet` for code-implementation subagents and the four algorithm-heavy game write-ups (section E); main session drives design, review and orchestration. Never opus subagents without written justification (global cost rule).
- **Infra over MCP (Render / Supabase / Sentry) is done by the main session, never a subagent** — a handful of calls, high stakes, and it needs the whole plan in context.
- **Call by ID, never list.** Resource IDs are recorded in "Current ground truth": use `get_service` / `get_postgres` / `get_project`. `list_services` returns all 13 services in the workspace (~5k tokens per call). Supabase `list_tables` with `verbose: false`; `list_logs` / `query_logs` only while debugging, tightly filtered.
- **Secrets never enter a Claude session** — not in chat, not as an MCP tool argument, not in a file Claude reads. The owner pastes them into dashboards or exports them in their own shell. This repo is public.
- **Don't delegate what is already in context.** Editing this plan is main-session work (one file, already loaded); spawning an agent means paying to re-read it.
- **Reviews:** one `/code-review medium` pass per code PR; docs-only PRs get none.
