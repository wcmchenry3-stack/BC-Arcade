name: Sync main → dev

on:
  push:
    branches: [main]

permissions:
  pull-requests: write
  contents: read

jobs:
  sync:
    uses: wcmchenry3-stack/.github/.github/workflows/called-sync-main-to-dev.yml@main
    secrets: inherit
