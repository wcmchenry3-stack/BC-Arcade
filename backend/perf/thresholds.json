{
  "_comment": "SLOs for Render free-tier deployment. Warm-up must complete before timing starts. See docs/PERFORMANCE.md.",
  "scenarios": {
    "game_flow": {
      "description": "One Yacht game through the session pipeline (single user, sequential)",
      "users": 1,
      "p95_ms": 500,
      "error_rate_pct": 0.0,
      "endpoints": [
        "POST /games",
        "POST /games/{id}/events",
        "PATCH /games/{id}/complete",
        "GET /games/{id}/rank"
      ]
    },
    "leaderboard": {
      "description": "Concurrent leaderboard reads (10 users)",
      "users": 10,
      "p95_ms": 300,
      "error_rate_pct": 1.0,
      "endpoints": ["GET /games/leaderboard/{game_type}"]
    },
    "stateless_reads": {
      "description": "Read-only polling (20 users)",
      "users": 20,
      "p95_ms": 200,
      "error_rate_pct": 0.0,
      "endpoints": [
        "GET /games/catalog",
        "GET /stats/me",
        "GET /games/me"
      ]
    }
  }
}
